
//this ui element is a selected svg image with an arrow select button which displays a overlay of svgs
const SvgSelect = () => {
    return (
        <div>

        </div>
    );
}
 
export default SvgSelect;